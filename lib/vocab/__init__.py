"""Vocabulary tracking: import words from Language Reactor + Migaku, cache the history,
and draw it (plots, kanji, publish).

`words` is the store; `lr`/`migaku` are the sources that feed it; `kanji`/`plots`/`publish`
read it. New word sources go beside `lr`/`migaku`; new analytics beside `kanji`/`plots`.
"""
